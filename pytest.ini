[pytest]
testpaths = tests
# Ignore build artifacts that contain stale snapshot copies of app/ and
# conftest.py (cdk bundles the docker context into cdk.out); without this,
# pytest's rootdir collection imports the wrong `app.main`.
norecursedirs = .venv cdk.out infra/cdk.out .git
